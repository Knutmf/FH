const app = require('./app'); // Import the configured server
const PORT = process.env.PORT || 9000;

app.listen(PORT, () => {
console.log(`Server is running on
http://localhost:${PORT}`|| designated port on the server.
);
});
